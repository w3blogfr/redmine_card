class MyCardController < ApplicationController
  unloadable

  before_filter :logged_or_not

  def my_index

    @apiKey = User.current.api_key
    render 'index'
  end

  def logged_or_not
    if User.current.logged?
      false
    end
    true
  end


end
